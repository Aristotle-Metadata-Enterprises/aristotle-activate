[project]
name = "activate"
version = "0.1.0"
description = ""
authors = [
    {name = "Aristotle Metadata", email="hello@aristotlemetadata.com"} 
]
readme = "README.md"

# requires-python = ">=3.12"
dependencies = [
    "azure-identity>=1.25.1",
    "click>=8.2.1",
    "lxml>=6.0.1",
    "msgraph-sdk>=1.46.0",
    "pyyaml>=6.0.2",
    "requests>=2.32.5",
    "sqlalchemy>=2.0.43",
    "turfpy>=0.0.8",
]

[project.scripts]
activateme = 'cli.activate.cli:activate_cli'
